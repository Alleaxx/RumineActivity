@inherits StatComponent
@page "/classic"
<style>
    .classic > header{
        display: flex;
        align-items: center;
        border-bottom: 1px solid gainsboro;
    }
    .classic header img{
        width: 24px;
        margin: 0 8px;
    }
    .classic h2{
        margin: 10px 0 4px 0;
    }
    .classic p{
        margin: 5px 0;
    }
    .info{
        text-align: right;
        color: gray;
        font-size: 0.8em;
    }
    .content{
        padding: 0.5em 1em;
    }
</style>

<section class="classic">
    <header>
        <img src="images/calendar.svg" />
        <h2>Классическая таблица</h2>
    </header>
    <div class="content">
        <p>
            Классика с Гитхаба / экселя.
        </p>
        <p>
            Может возникнуть вопрос – где, собственно, информация до июля 2014 и после июля 2021?<br />
            Ответ следующий: временно отсутствует, ждите ответного звонка и обновы.
        </p>
    </div>

    <p class="info">
        Периодический отчет: охватываемый период с 27 июля 2011 по @DateTime.Now.ToString("dd MMMM yyyy"), периодичность - месяц, источник - весь форум
    </p>

    <ReportView Report="Report" Mode="@(new ViewType(DViewTypes.Table))" />
</section>

@code {
    private StatisticsReport Report { get; set; } = ReportsFactory.CreateClassicReport();
}
